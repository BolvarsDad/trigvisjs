const canvas = document.querySelector(".canvas");
const ctx = canvas.getContext("2d");
const SIZE = 1000;
const TAU = 2 * Math.PI;o

let offsets;

function init_canvas() {
    canvas.width = SIZE;
    canvas.height = SIZE;
    ctx.fillStyle = "White";
    ctx.fillRect(0, 0, canvas.width, canvas.height);
}

function draw_numberline

init_canvas()
